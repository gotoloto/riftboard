# Python virtualenv + caches
.venv/
__pycache__/
*.pyc

# OS / editor cruft
.DS_Store
.idea/
.vscode/

# Local runtime artifacts
scrape.log
scrape-*.log
*.tmp
/decks.json  # only the root-level stray (legends/<slug>/decks.json must NOT be ignored)

# Personal collection workbooks dropped in for import
/collection-template_*.xlsx

# Claude Code workspace state (preview server config, plans, worktrees)
.claude/
